# Cloudflare Pages redirects for store subdomain
# This allows the store pages to access assets from the root directory

# Serve store pages from /store directory
/index.html /store/index.html 200
/success.html /store/success.html 200
/ /store/index.html 200

# Assets should be served from root /assets directory
# (no redirect needed - they're already at the correct path)
